# 五子棋联机房间（Vercel）

## 部署方式
- 直接把仓库推到 GitHub
- 在 Vercel 里导入该仓库并部署
- 默认会识别根目录静态页面与 `/api` 下的函数

## 使用方法
- 点击“创建房间”生成房间码
- 复制房间码发给朋友
- 朋友输入房间码加入，双方进入后自动开始

## 注意事项
- 当前房间数据保存在函数实例内存中，实例重启或扩缩容会导致房间失效
- 如需稳定联机，建议接入 Vercel KV/Upstash
